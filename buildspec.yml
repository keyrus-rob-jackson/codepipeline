version: 0.2

env:
  shell: bash
#  secrets-manager:

phases:
  install:
    commands:
      - echo $PATH
      - cat ~/.bashrc
      - source ~/.bashrc
      - source set_path.sh
      - ./set_path.sh
      - echo $PATH
      - echo $SHELL
      - whoami
      - pwd
      - composer update
      - composer install
      - npm i
  build:
    commands:
      - php artisan key:generate
      - php artisan config:cache
      - npm run build

  post_build:
    commands:
      - echo "Post Build"

artifacts:
  files: '**/*'
